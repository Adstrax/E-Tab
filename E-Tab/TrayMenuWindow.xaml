<Window x:Class="ETab.TrayMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="E-Tab"
        Width="264"
        Height="214"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="True"
        Topmost="True"
        WindowStartupLocation="Manual"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        Deactivated="OnDeactivated">
    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource EtabTextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="12,9"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource EtabHoverOverlay}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource EtabPressedOverlay}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Win11ToggleStyle" TargetType="ToggleButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Track"
                                    Width="42"
                                    Height="22"
                                    CornerRadius="11"
                                    Background="{DynamicResource EtabToggleTrack}"
                                    BorderBrush="{DynamicResource EtabToggleBorder}"
                                    BorderThickness="1"/>
                            <Ellipse x:Name="Thumb"
                                     Width="16"
                                     Height="16"
                                     Fill="{DynamicResource EtabToggleThumb}"
                                     HorizontalAlignment="Left"
                                     Margin="3,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#0067C0"/>
                                <Setter TargetName="Track" Property="BorderBrush" Value="#0067C0"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8"
            Background="{DynamicResource EtabMenuOverlay}"
            BorderBrush="{DynamicResource EtabMenuBorderBrush}"
            BorderThickness="1"
            Padding="6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24"
                              ShadowDepth="8"
                              Direction="270"
                              Color="#000000"
                              Opacity="0.45"/>
        </Border.Effect>
        <StackPanel>
            <Grid Margin="8,10,8,8">
                <StackPanel Orientation="Horizontal">
                    <Image Width="28"
                           Height="28"
                           Source="pack://application:,,,/Icon.ico"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="E-Tab"
                                   Foreground="{DynamicResource EtabTextPrimary}"
                                   FontSize="13"
                                   FontWeight="SemiBold"/>
                        <TextBlock x:Name="VersionText"
                                   Text="v2.1.0"
                                   Foreground="{DynamicResource EtabTextSecondary}"
                                   FontSize="11"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <Border Height="1" Background="{DynamicResource EtabSeparator}" Margin="8,0"/>

            <StackPanel Orientation="Horizontal" Margin="12,10,0,10">
                <Ellipse Width="8"
                         Height="8"
                         Fill="{DynamicResource EtabAccent}"
                         VerticalAlignment="Center"/>
                <TextBlock Text="Active · Folders open in new tabs"
                           Foreground="{DynamicResource EtabTextOnPill}"
                           FontSize="12.5"
                           Margin="10,0,0,0"/>
            </StackPanel>

            <Border Height="1" Background="{DynamicResource EtabSeparator}" Margin="8,0"/>

            <Grid Margin="12,9,12,9">
                <TextBlock Text="Start with Windows"
                           Foreground="{DynamicResource EtabTextPrimary}"
                           FontSize="13"
                           VerticalAlignment="Center"/>
                <ToggleButton x:Name="AutoStartToggle"
                              Style="{StaticResource Win11ToggleStyle}"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              AutomationProperties.Name="Start with Windows"
                              AutomationProperties.AutomationId="AutoStartToggle"
                              Checked="AutoStartToggle_Changed"
                              Unchecked="AutoStartToggle_Changed"/>
            </Grid>

            <Border Height="1" Background="{DynamicResource EtabSeparator}" Margin="8,0"/>

            <Button x:Name="SettingsButton"
                    Content="Settings"
                    Style="{StaticResource MenuButtonStyle}"
                    Margin="0,4,0,0"
                    AutomationProperties.Name="Settings"
                    AutomationProperties.AutomationId="SettingsButton"
                    Click="SettingsButton_Click"/>
            <Button x:Name="ExitButton"
                    Content="Exit"
                    Style="{StaticResource MenuButtonStyle}"
                    AutomationProperties.Name="Exit"
                    AutomationProperties.AutomationId="ExitButton"
                    Click="ExitButton_Click"/>
        </StackPanel>
    </Border>
</Window>
