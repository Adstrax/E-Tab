<Window x:Class="ETab.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="E-Tab Settings"
        Width="440"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        MouseLeftButtonDown="OnTitleDrag">
    <Window.Resources>
        <Style x:Key="Win11CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource EtabTextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource EtabHoverOverlay}"/>
                                <Setter Property="Foreground" Value="{DynamicResource EtabTextPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource EtabPressedOverlay}"/>
                                <Setter Property="Foreground" Value="{DynamicResource EtabTextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Win11ToggleStyle" TargetType="ToggleButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="Track"
                                    Width="42"
                                    Height="22"
                                    CornerRadius="11"
                                    Background="{DynamicResource EtabToggleTrack}"
                                    BorderBrush="{DynamicResource EtabToggleBorder}"
                                    BorderThickness="1"/>
                            <Ellipse x:Name="Thumb"
                                     Width="16"
                                     Height="16"
                                     Fill="{DynamicResource EtabToggleThumb}"
                                     HorizontalAlignment="Left"
                                     Margin="3,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#0067C0"/>
                                <Setter TargetName="Track" Property="BorderBrush" Value="#0067C0"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8"
            Background="{DynamicResource EtabWindowOverlay}"
            BorderBrush="{DynamicResource EtabBorderBrush}"
            BorderThickness="1"
            Padding="22">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24"
                              ShadowDepth="6"
                              Direction="270"
                              Color="#000000"
                              Opacity="0.35"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Width="34"
                           Height="34"
                           Source="pack://application:,,,/Icon.ico"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Text="E-Tab Settings"
                                   Foreground="{DynamicResource EtabTextPrimary}"
                                   FontSize="16"
                                   FontWeight="SemiBold"/>
                        <TextBlock Text="Open folders in new tabs"
                                   Foreground="{DynamicResource EtabTextSecondary}"
                                   FontSize="12"
                                   Margin="0,3,0,0"/>
                    </StackPanel>
                </StackPanel>

                <Button x:Name="CloseButton"
                        Content="&#x2715;"
                        Width="32"
                        Height="28"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Style="{StaticResource Win11CloseButtonStyle}"
                        FontSize="14"
                        Click="CloseButton_Click"/>
            </Grid>

            <Border Grid.Row="1"
                    Background="{DynamicResource EtabPillBackground}"
                    BorderBrush="{DynamicResource EtabPillBorder}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="12,10"
                    Margin="0,18,0,0">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="8"
                             Height="8"
                             Fill="{DynamicResource EtabAccent}"
                             VerticalAlignment="Center"/>
                    <TextBlock Text="Active · New folder windows become tabs"
                               Foreground="{DynamicResource EtabTextOnPill}"
                               FontSize="13"
                               Margin="10,0,0,0"/>
                </StackPanel>
            </Border>

            <Grid Grid.Row="2" Margin="0,22,0,0">
                <StackPanel>
                    <TextBlock Text="Start with Windows"
                               Foreground="{DynamicResource EtabTextPrimary}"
                               FontSize="14"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="Run E-Tab in the tray after signing in"
                               Foreground="{DynamicResource EtabTextSecondary}"
                               FontSize="12"
                               Margin="0,4,0,0"/>
                </StackPanel>

                <ToggleButton x:Name="AutoStartToggle"
                              Style="{StaticResource Win11ToggleStyle}"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Center"
                              AutomationProperties.Name="Start with Windows"
                              AutomationProperties.AutomationId="AutoStartToggle"
                              Checked="AutoStartToggle_Changed"
                              Unchecked="AutoStartToggle_Changed"/>
            </Grid>

            <TextBlock x:Name="VersionText"
                       Grid.Row="3"
                       Text="E-Tab v1.5.2"
                       Foreground="{DynamicResource EtabTextMuted}"
                       FontSize="11"
                       Margin="0,24,0,0"/>
        </Grid>
    </Border>
</Window>
